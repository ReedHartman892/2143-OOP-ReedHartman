# Polymorphism in C++
The ability o a single interface, like a function name or an operator, to represent different behaviors depending on the context or data types involved.

It is categorized into two main types: **Compile-time** (Static) and **Runtime** (Dynamic).