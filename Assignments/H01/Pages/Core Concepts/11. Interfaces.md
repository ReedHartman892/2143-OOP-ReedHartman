# Interfaces in C++
There is no specific `interface` keyword in C++ like there is in Java or C#. Instead, interfaces are implied using **Abstract Classes** containing only **Pure Virtual Functions**.

