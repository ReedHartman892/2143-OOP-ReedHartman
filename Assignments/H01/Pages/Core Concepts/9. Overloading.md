# Overloading in C++
**Overloading** allows you to define multiple functions or operators with the same name within the same scope, provided they have different **function signatures**.

This is a form of **compile-time polymorphism**, where the compiler determines the correct version to invoke based on the arguments provided.

## Overloading vs Overriding
These are different concepts, fundamentally.